{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:vue-button": "lerna run --scope vue-button",
    "build:vue-button": "lerna run --scope vue-button build",
    "test:vite-common": "lerna run --scope vite-common test",
    "build": "lerna run build"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.1.0",
    "lerna": "^7.1.1",
    "prop-types": "^15.8.1",
    "typescript": "^5.0.2",
    "vite": "^4.3.9",
    "vue-tsc": "^1.4.2"
  }
}
